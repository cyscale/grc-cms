{
  "name": "Ensure that AWS Lambda functions do not share the same AWS IAM execution role",
  "slug": "aws-iam-21",
  "body": "### Security intent\n\nThis control validates that **that AWS Lambda functions do not share the same AWS IAM execution role** across AWS.\n\n### Access condition reviewed\n\nThe assessment focuses on aWS Lambda functions sharing the same AWS IAM execution role. It reviews the identity, credential, role, or policy state that creates the access condition described by the control.\n\n### Practitioner note\n\nIdentity findings deserve careful cleanup rather than blind deletion. Confirm ownership and business need, remove unnecessary standing access, and prefer federation, groups, roles, MFA, and short-lived credentials over direct long-lived grants.",
  "description": "AWS Lambda functions sharing the same AWS IAM execution role",
  "severity": 2,
  "remediationDescription": "## AWS remediation\n\n### Console\n1. Identify Lambda functions sharing an execution role.\n2. Create one least-privilege role per function.\n3. Update each function to use its dedicated role.\n\n### AWS CLI\n~~~bash\naws lambda update-function-configuration \\\n  --function-name <function-name> \\\n  --role arn:aws:iam::<account-id>:role/<dedicated-lambda-role>\n~~~\n\n### References\n- https://docs.aws.amazon.com/lambda/latest/dg/lambda-intro-execution-role.html\n- https://docs.aws.amazon.com/cli/latest/reference/lambda/update-function-configuration.html",
  "rules": [
    {
      "cloudProviders": [
        "aws"
      ],
      "comparator": "eq",
      "expectedResult": "[]",
      "subjects": [
        "Function"
      ],
      "query": "aws-iam-21-aws-lambda-functions-do-not-share-the-same-aws-iam-execution-role",
      "returnPath": ""
    }
  ]
}
