{
  "name": "Ensure IAM Users that are inactive for 30 days or more are deactivated",
  "slug": "aws-iam-20",
  "body": "IAM users who have not logged into AWS and have no API activity for 30 days should be considered inactive.\n\nWe recommend IAM users that are not active for 30 days or more have their accounts terminated.",
  "description": "IAM Users that are inactive for 30 days or more are deactivated",
  "severity": 2,
  "remediationDescription": "If you conclude that the users can be deleted, follow the [AWS Documentation](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_users_manage.html#id_users_deleting) describing the steps.\n\n## Service-wide remediation\n\n> **Recommended when many resources are affected:** fix the platform baseline first so new resources inherit the secure setting, then remediate the existing flagged resources in batches.\n\n### AWS\nUse AWS Organizations guardrails, AWS Config rules or conformance packs where they fit, approved account baselines, and IaC modules so new resources inherit the secure setting.\n\n## Operational rollout\n\n1. Fix the baseline first at the account, subscription, project, cluster, or tenant scope that owns this control.\n2. Remediate the currently affected resources in batches, starting with internet-exposed and production assets.\n3. Re-scan and track approved exceptions with an owner and expiry date.",
  "rules": [
    {
      "cloudProviders": [
        "aws"
      ],
      "comparator": "eq",
      "expectedResult": "[]",
      "subjects": [
        "IAMUser"
      ],
      "query": "aws-iam-20-iam-users-that-are-inactive-for-30-days-or-more-are-deactivated",
      "returnPath": ""
    }
  ]
}
